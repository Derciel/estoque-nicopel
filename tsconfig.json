// tsconfig.json (ou jsconfig.json)
{
  "compilerOptions": {
    // ... (outras configurações)
    "baseUrl": ".",
    "paths": {
      // ✅ Defina o alias $lib para a pasta src/lib
      "$lib/*": ["src/lib/*"],
      // ✅ Defina o alias @ para a pasta src
      "@/*": ["src/*"]
    }
  }
}